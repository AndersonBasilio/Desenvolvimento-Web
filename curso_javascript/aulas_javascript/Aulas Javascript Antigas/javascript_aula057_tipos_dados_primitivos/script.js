//Tipos de dados primitivos são string, number, undefined, null, boolean e symbol
const nome = 'Luiz'; //string
const nome1 = 'Anderson'; //string
const nome2 = 'Guanabara'; //string
const num1 = 10; //number
const num2 = 10.52; //number

// Usando let 
let nomeAluno; //undefined -> Não aponta para nenhum local na memória
const sobrenomeAluno = null; // Nulo -> Não aponta para nenhum local na memória
const aprovado= true // true ou false

console.log(`O tipo primitivo é ${typeof nome} e o valor é ${nome}`);

